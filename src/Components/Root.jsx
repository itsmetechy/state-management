import React from 'react'
import Header from './Header'

const Root = (props) => {
        return (
            <div>
                <div> {'Hi'} </div>
                {/* <div>{ props.children  }</div> */}
            </div>
        )
    }

export default Root
